define(function(Manip) {
  var EventHelper = {};

  EventHelper.addClickListener = function(id, callback) {
    
  }

  EventHelper.addPurchasableSwitch = function(switchId, containerId) {
    
  }

  return EventHelper;
})
