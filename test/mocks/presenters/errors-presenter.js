define(function() {
  function MockErrorsPresenter(object) {
    // stubbed constructor
  }

  MockErrorsPresenter.prototype.prepend = function(error) {
    // stubbed method
  }

  return MockErrorsPresenter;
})
